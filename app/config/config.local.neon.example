parameters:
	doctrine:
		database:
			driver: pdo_mysql
			user:
			password:
			dbname:
		autogenerateProxyClasses: TRUE
	security:
		salt:

nette:
	container:
		debugger: TRUE

services:

factories:
